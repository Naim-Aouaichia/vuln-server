name: Run security Scan

on:
  push:
    branches:
      - main
      - develop
      - preprod
      - 'feat/**'

jobs:
  sast_scan:
    uses: naouaichRepo/orga-reusable-workflows/.github/workflows/security-reusable-sast-scan.yml@v2
  sca_scan:
    uses: naouaichRepo/orga-reusable-workflows/.github/workflows/security-reusable-sca-scan.yml@v2