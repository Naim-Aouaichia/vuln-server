name: Run Semgrep reusable scan

on:
  push:
    branches:
      - main
      - develop
      - preprod
      - 'feat/**'

jobs:
  sast_scan:
    uses: naouaichRepo/orga-reusable-workflows/.github/workflows/security-reusable-sast-scan.yml